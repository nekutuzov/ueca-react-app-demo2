# Welcome to UECA-React Demo

This application is built entirely with UECA-React principles and patterns.

**[Open Application Architecture Documentation →](/home/architecture)**

---

## 🚀 What is UECA-React?

UECA-React extends React with explicit component IDs, automatic two-way property bindings, controlled lifecycle hooks, reactive state, auto-defined events, and a built-in message bus. These features provide the clarity and control large, complex web applications require.

---

## ⚡ Why It Excels at Scale

- **Predictable architecture**: Every component has a unique ID and clear mental model. No hidden dependencies — teams read and maintain the code with ease.
- **Zero-boilerplate data flow**: Automatic property bindings sync state to UI instantly. Changes propagate reliably without manual setters.
- **Controlled lifecycle**: Precise hooks — `constr`, `init`, `deinit`, `mount`, `unmount` — give full control over creation, activation, cleanup, and DOM lifecycle.
- **Auto-defined events**: For every bound property, `onChange<Prop>` and `onChanging<Prop>` events are generated automatically, enabling clean reactive logic.
- **Stable IDs for testing**: Component IDs produce consistent HTML IDs, perfect for automated UI testing with tools like UI Vision.
- **Decoupled communication**: The message bus allows components to interact without tight coupling — ideal for dashboards, forms, and multi-module systems.

**The result**: faster development, fewer bugs, and effortless scaling even in large codebases.

---

## 🤖 AI-Friendly Architecture

This architecture is well understood by AI. This demo application was developed with AI assistance — 80% of the source code was generated by an AI Agent.

---

## 🎯 Explore the Demo

Explore the demo. Every feature you see follows the UECA-React way.